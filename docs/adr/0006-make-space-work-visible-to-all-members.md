# Make space work visible to all members

Every current Member of a Space can see its Board, Tasks, Subtasks, comments, assignments, and Task history. Dig has no private Tasks, restricted Columns, or field-level permissions. This preserves one truthful flow view and keeps authorization at the Space boundary instead of creating hidden work inside it.
