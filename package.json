{
  "name": "dig-tracker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "npm run build:api && node --env-file-if-exists=.env.local api-dist/server.js",
    "build": "tsc -b && npm run build:api && vite build",
    "build:api": "tsc -p tsconfig.api.json",
    "migrate": "npm run build:api && node --env-file-if-exists=.env.local api-dist/migrate.js up",
    "start:api": "node api-dist/server.js",
    "lint": "eslint .",
    "preview": "vite preview",
    "test": "vitest run",
    "test:db": "./scripts/test-db",
    "test:db:running": "npm run build:api && node api-dist/migrate.js up && DIG_DATABASE_TESTS=1 vitest run api/repository.integration.test.ts",
    "test:watch": "vitest",
    "container:build": "podman build -t dig-tracker -f Containerfile .",
    "container:run": "podman run --rm -p 127.0.0.1:8080:8080 dig-tracker",
    "db:up": "./scripts/compose -f podman-compose.yml up -d db",
    "db:migrate": "npm run migrate",
    "db:reset": "npm run build:api && node --env-file-if-exists=.env.local api-dist/migrate.js reset",
    "db:backup": "./scripts/compose -f podman-compose.yml exec -T db pg_dump -U dig -d dig -Fc > dig-backup.dump",
    "db:restore": "./scripts/compose -f podman-compose.yml exec -T db pg_restore -U dig -d dig --clean --if-exists < dig-backup.dump",
    "db:down": "./scripts/compose -f podman-compose.yml down"
  },
  "dependencies": {
    "pg": "^8.20.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^24.10.1",
    "@types/pg": "^8.20.0",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@types/uuid": "^10.0.0",
    "@vitejs/plugin-react": "^5.1.1",
    "eslint": "^9.39.1",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.4.24",
    "globals": "^16.5.0",
    "jsdom": "^28.1.0",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.48.0",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
